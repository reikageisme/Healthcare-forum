# G5–G11 implementation evidence

Date: 2026-09-07 (Asia/Bangkok).
Implementation base: 363d62b4224d0cdd7bbdcdd26ae5baf8ed4247df on
dev/healthcare-forum-hardening, with the existing G3/G4 worktree changes
preserved.

Status: **G5–G11 code, test and configuration gates pass locally.** The
production PostgreSQL/Docker runtime and rollback drill remain external gates,
so this record does not claim a release deployment.

## Completed groups

| Group | Delivered |
| --- | --- |
| G5 | Narrowed the backend TipTap allowlist, enforced HTTP(S)/uploads URL policy, rejected empty-after-cleaning content and unsafe thumbnails, sanitized legacy post/comment full-content responses, filtered legacy image extraction, added DOMPurify render defense, and made moderation preview load full detail before rendering. |
| G6 | Tag detail/hot counts and ordering now use filtered posts.id rows; zero-public-count tags remain visible with deterministic name ordering. |
| G7 | Report target mutation and all matching open-report closure now run inside one transaction. Already-absent targets close idempotently with target_exists=false; closed reports return 409. Frontend mutation fallbacks were removed and the report UI distinguishes open/resolved/dismissed states. |
| G8 | Added ESLint 8 configuration, shared pagination controls, real total/page state for admin lists, committed frontend and backend lockfiles, focused G5/G7/G8 tests, and npm ci Docker installation. Strict compiler flags remain enabled. |
| G9 | Vite and Nginx /uploads proxy paths are present; Nginx uses ^~ before the image-extension regex. The smoke script can compare uploaded bytes through one or more asset origins. |
| G10 | Production config now fails fast on missing/development DB URLs, weak/default JWT secrets and wildcard/invalid CORS. DB_ECHO is canonical, startup remains createDatabase → migrate → sanitizeExisting → createAdmin, optional dev .env loading is supported, local DB health checks are parameterized, and empty new databases fail without ADMIN_PASSWORD. |
| G11 | Added clean backend/frontend CI, PostgreSQL enum/Drizzle verification, PowerShell end-to-end smoke, and operator backup/rollback guidance. |

The recommended G0 visibility/sanitizer/token policy defaults are now encoded in
the implementation and evidence; this is a scheduling decision for the
requested work, not proof that production rollout approval or rollback has
occurred.

## Verification

Runtime: Node 22.23.1, npm 10.9.8, backend Vitest 2.1.9, frontend Vitest
0.34.6, TypeScript, PGlite and installed lockfile dependencies.

| Check | Result |
| --- | --- |
| Backend full suite | PASS: 359 tests across 12 files. |
| Backend typecheck/build | PASS. |
| Frontend lint | PASS with zero warnings. |
| Frontend typecheck/build | PASS; existing large bundle warning remains (1,309.80 kB main JS / 385.22 kB gzip). |
| Frontend full suite | PASS: 37 tests across 12 files. |
| Clean frontend npm ci | PASS: 574 packages installed from frontend/package-lock.json. |
| Clean backend npm ci | PASS: 112 packages installed from backend/package-lock.json. |
| Development Compose config | PASS. |
| Local-PostgreSQL Compose overlay config | PASS. |
| Production Compose config with explicit safe variables | PASS. |
| Production Compose config with required variables blank | PASS fail-fast: DATABASE_URL, JWT_SECRET and BACKEND_CORS_ORIGINS are required. |
| G10 production config tests | PASS: 10 tests. |
| G11 PowerShell smoke syntax parse | PASS. |
| npm audit --omit=dev | Reviewed: 29 moderate, 0 high, 0 critical. Available remediation is major-version TipTap/React Router migration; no blind force upgrade applied. |
| git diff --check | PASS. |

Focused additions include 5 sanitizer tests, 3 tag tests, 7 backend report
tests, 10 config tests, 195 visibility tests from G4, and 13 frontend
pagination/report/sanitizer tests beyond the earlier G3 coverage.

## External gates not claimed

- Docker Desktop was not running on this host, so container startup, live Vite
  proxy retrieval and live Nginx retrieval were not executed.
- No reachable production PostgreSQL instance was used for the final migration
  or rollback drill. CI now contains a PostgreSQL 16 service job and the
  verify:postgres script for that gate.
- The PowerShell smoke script is ready at
  scripts/smoke-healthcare-forum.ps1 and requires a running stack plus a
  moderator/admin credential. It was syntax-checked but not run against a
  live service.
- GitNexus CLI/MCP/index and the descriptor-anchored plan reader were
  unavailable on this Windows host. No graph/PDG impact or graph-backed
  commit gate is claimed; changes remain uncommitted for review.
